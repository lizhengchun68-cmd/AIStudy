---
description: 有限元仿真项目三层架构、句柄接口与模块协作强制规范
alwaysApply: true
---

# 有限元仿真项目开发强制规则

## 1. 架构规范

采用三层分层架构：**原生内核层**、**适配封装层**、**流程调度层**。

- 严格高内聚、低耦合；按有限元仿真功能模块化拆分。
- 禁止模块间无序强依赖。
- 所有设计预留未来兼容 **AI Agent 调度** 的扩展能力。

## 2. 指针与接口强制约束

| 范围 | 允许 |
|------|------|
| 底层内核内部 | 裸指针、结构体指针、函数指针、回调 |
| 对外公开接口、模块间调用 | **严禁**裸指针、**严禁**函数指针 |

- 内存对象统一用**字符串/数字句柄 ID**对外标识，替代指针外传。
- 回调逻辑：外部只传**语义描述配置结构体**，禁止传递函数地址。

```c
// ❌ 禁止：对外暴露指针或函数指针
void fem_export_mesh(Mesh* mesh, void (*on_done)(int));

// ✅ 正确：句柄 + 配置结构体
int fem_create_mesh(const char* path);
int fem_export_mesh(const char* mesh_handle_id, const ExportConfig* cfg);
```

## 3. 模块设计规范

每个模块必须包含：

1. **底层原生实现**
2. **无指针对外封装接口**

对外接口只使用：基础数据类型、普通结构体、资源句柄 ID。设计上可平滑适配未来 **JSON 配置**、**独立 EXE 部署**。

## 4. 编码约束

- 遵循单一职责原则，每个函数只做一件事
- 不滥用全局变量，模块间状态隔离
- 新增功能先定义「模块职责+输入输出接口」，再写实现代码
- 不得擅自扩展整体架构、修改分层规则

## 5. 协作规则

开发任何仿真相关模块时：

- 开发任何新模块，先输出模块拆分+接口定义，再提供实现代码
- 若需求与架构规则冲突，先向我确认，不得自行修改既定规范

若需求与架构规则冲突，**先向用户确认**，不要自行改动既定架构规范。
